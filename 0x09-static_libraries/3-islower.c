#include "main.h"

/**
 * _islower - checks for lowercase character
 * @num: the character to check
 * Return: 1 if num is lowercase, 0 otherwise
 */


int _islower(int num)
{
	return (num >= 'a' && num <= 'z');
}

